# 📘 Day 3 – Variables

## 📌 Topics Covered

* What is variable
* Declaring variables (`var`, `let`, `const`)
* Variable naming rules
* Assigning values
* Reassignment
* Redeclaration
* Scope (basic)

---

## 🧪 Practice

### 1. Create Variables

* Create some variables with different names
* Use `let`, `const`, and `var`

---

### 2. Reassignment

* Create variables using `let`
* Change their values multiple times

---

### 3. Const Check

* Create variables using `const`
* Try to reassign and observe error

---

### 4. Redeclaration

* Try redeclaration with:

  * `var`
  * `let`
  * `const`

---

### 5. Scope Check

* Create variable inside `{ }`
* Try accessing outside

---

## 🎯 Tasks

* Use meaningful variable names
* Use camelCase
* Avoid `var` (prefer `let` and `const`)

---

